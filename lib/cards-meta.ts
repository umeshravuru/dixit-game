// Constants used by both client and server. Kept separate from cards.ts
// so the server doesn't need the image-URL list at runtime.
export const HAND_SIZE = 6;
export const CARD_COUNT = 100; // must match cards.ts length
